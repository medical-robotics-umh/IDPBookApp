<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IDPBookApp.Pages.NuevaEncuestaPage"
             xmlns:viewmodel="clr-namespace:IDPBookApp.ViewModel"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:model="clr-namespace:IDPBookApp.Models"
             x:DataType="viewmodel:NuevaEncuestaViewModel"
             Title="NuevaEncuestaPage">
    <ScrollView>
        <VerticalStackLayout VerticalOptions="Fill">
            
            <ImageButton Source="back2" Aspect="Fill" WidthRequest="30" HeightRequest="30" HorizontalOptions="Start" Margin="20,20,0,0" IsEnabled="{Binding ValidCuest}" Command="{Binding BackBtnCommand}">
                <ImageButton.Behaviors>
                    <toolkit:IconTintColorBehavior TintColor="#28B463" />
                </ImageButton.Behaviors>
            </ImageButton>
            <Label Text="NUEVO CUESTIONARIO" FontSize="Large" FontAttributes="Bold" HorizontalOptions="Center" Margin="0,-30,0,0"/>

            <Border Margin="12,20" Padding="10,10">
                <Grid ColumnDefinitions="*" RowDefinitions="*,*,*,*,*,*,*,*,*,*,*,*,auto,*,*,auto,*,auto,80" RowSpacing="5">

                    <Label VerticalOptions="Center" Text="Movilidad:" HorizontalOptions="Start" FontAttributes="Bold"/>
                    <Picker SelectedIndex="{Binding QMovil}" Grid.Row="1" Title="▼ Selecciona una opción" HeightRequest="35"  HorizontalOptions="Fill" >
                        <Picker.Items>
                            <x:String>No tengo problemas para caminar.</x:String>
                            <x:String>Tengo algunos problemas para caminar.</x:String>
                            <x:String>Tengo que estar en la cama.</x:String>
                        </Picker.Items>
                    </Picker>

                    <Label Grid.Row="2" VerticalOptions="Center" Text="Cuidado personal:" HorizontalOptions="Start" FontAttributes="Bold" />
                    <Picker SelectedIndex="{Binding QCuid}" Grid.Row="3" Title="▼ Selecciona una opción" HeightRequest="35"  HorizontalOptions="Fill" >
                        <Picker.Items>
                            <x:String>No tengo problemas con el cuidado personal.</x:String>
                            <x:String>Tengo algunos problemas para levantarme o vestirme solo/a.</x:String>
                            <x:String>Soy incapaz de lavarme y vestirme solo/a.</x:String>
                        </Picker.Items>
                    </Picker>

                    <Label Grid.Row="4" VerticalOptions="Center" Text="Actividades diarias:" FontAttributes="Bold" HorizontalOptions="Start" />
                    <Picker SelectedIndex="{Binding QActDia}" Grid.Row="5" Title="▼ Selecciona una opción" HeightRequest="35"  HorizontalOptions="Fill"  FontAutoScalingEnabled="True">
                        <Picker.Items>
                            <x:String>No tengo problemas para realizar mis actividades diarias.</x:String>
                            <x:String>Tengo algunos problemas para realizar mis actividades diarias.</x:String>
                            <x:String>Soy incapaz de realizar mis actividades diarias.</x:String>
                        </Picker.Items>
                    </Picker>

                    <Label Grid.Row="6" VerticalOptions="Center" Text="Dolor/Malestar:" FontAttributes="Bold" HorizontalOptions="Start" />
                    <Picker SelectedIndex="{Binding QDolor}" Grid.Row="7" Title="▼ Selecciona una opción"  HeightRequest="35"  HorizontalOptions="Fill" >
                        <Picker.Items>
                            <x:String>No tengo dolor ni malestar.</x:String>
                            <x:String>Tengo moderado dolor o malestar.</x:String>
                            <x:String>Tengo mucho dolor o malestar.</x:String>
                        </Picker.Items>
                    </Picker>

                    <Label Grid.Row="8" VerticalOptions="Center" Text="Ansiedad/Depresión:" FontAttributes="Bold" HorizontalOptions="Start" />
                    <Picker SelectedIndex="{Binding QAnsd}" Grid.Row="9" Title="▼ Selecciona una opción" HeightRequest="35"  HorizontalOptions="Fill" >
                        <Picker.Items>
                            <x:String>No estoy ansioso/a ni deprimido/a.</x:String>
                            <x:String>Estoy moderadamente ansioso/a o deprimido/a.</x:String>
                            <x:String>Estoy muy ansioso/a o deprimido/a.</x:String>
                        </Picker.Items>
                    </Picker>

                    <Label Grid.Row="10" VerticalOptions="Center" Text="Su estado de salud hoy:" FontAttributes="Bold" HorizontalOptions="Start" />
                    <HorizontalStackLayout Spacing="5" Grid.Row="11">
                        <Label Text="Por favor, deslice la figura (" VerticalOptions="Center" FontAttributes="Italic"/>
                        <Image Source="thumb" HeightRequest="25" WidthRequest="25" VerticalOptions="Center"/>
                        <Label Text=") hasta el valor que en su" VerticalOptions="Center" FontAttributes="Italic"/>
                    </HorizontalStackLayout>
                    <Label Grid.Row="12" VerticalOptions="Center" Text="opinion, indique lo bueno o malo que es su estado de salud el día de hoy." FontAttributes="Italic" HorizontalOptions="Start" Margin="0,-8,0,0"/>
                    <Label Grid.Row="13" VerticalOptions="Center" Text="{Binding Source={x:Reference slider},Path=Value,StringFormat='Valor: {0:F0}'}"  HorizontalOptions="Center" FontAttributes="Bold"/>
                    <Slider Grid.Row="14" Minimum="0" Maximum="100" HorizontalOptions="Fill" ThumbImageSource="thumb" MaximumTrackColor="White" x:Name="slider" Value="{Binding QEscala}"/>
                    <Image Grid.Row="15" Source="regla.png" Aspect="AspectFit"/>

                    <Label Grid.Row="16" VerticalOptions="Center" Text="Describe brevemente el porque de tu puntuación:" FontAttributes="Bold" HorizontalOptions="Start" />
                    <Editor Text="{Binding QDesc}" Grid.Row="16" Placeholder="Descripción" HorizontalTextAlignment="Start" HorizontalOptions="Fill" HeightRequest="100" />

                    <Button Grid.Row="18"  HorizontalOptions="Center" Margin="-100,0,0,0" Text="Agregar" WidthRequest="90" HeightRequest="45" Command="{Binding NewCuestCommand}"/>
                    <Button Grid.Row="18"  HorizontalOptions="Center" Margin="100,0,0,0" Text="Salir" BackgroundColor="#E67E22" WidthRequest="90" HeightRequest="45" Command="{Binding BackBtnCommand}" IsEnabled="{Binding ValidCuest}"/>
                </Grid>
            </Border>
            
            <ActivityIndicator IsRunning = "{Binding Run}" Margin = "10" HorizontalOptions = "Center" />
            
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>